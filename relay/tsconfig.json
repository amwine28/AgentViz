{
  "compilerOptions": {
    "target": "ES2022",
    "module": "CommonJS",
    "outDir": "dist",
    "rootDir": "src",
    "strict": true,
    "esModuleInterop": true
  },
  "include": ["src"],
  "comment": "replay-claude-code.ts imports the shared ui/ translator across packages and is run via ts-node --transpile-only, never built into dist; exclude it so tsc's rootDir check passes.",
  "exclude": ["src/replay-claude-code.ts"]
}
